# backend 构建上下文瘦身（构建在仓库根 context 进行）：
# 排除本地依赖/产物/密钥，避免进入镜像层与构建上下文传输。
.git
.design
frontend
storage
.env
.env.local
# 本地缓存（npm/Go/Playwright 等工作区内缓存目录，体积大且与构建无关；
# 曾致 WSL 下 build context readdirent ENOMEM）。
.cache
.npm-cache
.tmp
